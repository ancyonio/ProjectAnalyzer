CREATE OR REPLACE FUNCTION ORDER_APP.ORDER_TOTAL(p_order_id NUMBER) RETURN NUMBER AS
  l_total NUMBER;
BEGIN
  SELECT /*+ FULL(l) */ SUM(l.QUANTITY)
    INTO l_total
    FROM ORDER_APP.ORDER_LINES l
   WHERE l.ORDER_ID = p_order_id;
  RETURN NVL(l_total, 0);
END;
