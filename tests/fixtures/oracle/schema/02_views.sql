-- A view over two tables: exercises DEPENDS_ON view lineage.
CREATE OR REPLACE VIEW ORDER_APP.OPEN_ORDERS_V AS
SELECT o.ORDER_ID, o.CUSTOMER_ID, c.NAME, o.ORDER_TOTAL
  FROM ORDER_APP.ORDERS o
  JOIN ORDER_APP.CUSTOMERS c ON c.CUSTOMER_ID = o.CUSTOMER_ID
 WHERE o.STATUS_CODE = 'OPEN';
